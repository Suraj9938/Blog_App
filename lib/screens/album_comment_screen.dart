import 'package:flutter/material.dart';

class AlbumCommentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Album and Comment"),
    );
  }
}
